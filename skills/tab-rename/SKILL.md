---
description: Rename the terminal tab to show the current worktree branch name and ports
---

When the user wants to rename their tab, set the tab title, or fix the tab name, run the `/vandra-dev:cmd-tab-rename` command.
